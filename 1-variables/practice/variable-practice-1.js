// 1. Declare a new variable and set it equal to the string of your name
const myName = "Amy";
const nameLog = `My name is ${myName}`;

console.log(nameLog);
// 2. Declare a new variable and set it equal to a number
const myNum = 12;
const numLog = `Here is a number ${myNum}`;

console.log(numLog);
// 3. Set a variable to a boolean value
const myBool = false;

const boolLog = `Here is a boolean: ${myBool}`;
// 4. Create a variable called carName, assign the value Volvo to it.
const carName = "Volvo";
const carLog = `Let's log a car ${carName}`;

console.log(carLog);
// 5. Create a variable called num, assign the value 50 to it.
const num = 50;
const fiftyLog = `The number is ${num}!`;

console.log(fiftyLog);
// 6. Display the sum of 5 + 10, using two variables: num1 and num2.
const num1 = 5;
const num2 = 3;
const sum = num1 + num2;

console.log(`${num1} plus ${num2} equals ${sum}.`);
// 7. Create a variable called num3, assign the addition of num1 and num2 to it.
const num3 = sum;
